import React from 'react'

export const InterestsElement = ({ props }) => {
    /* Тут рендерится контейнер с интересом Погода, Посещаемое и т.п. */
    const {header, body} = props
  return (
    <div>InterestsElement</div>
  )
}
